CREATE TABLE `shoppingshow`.`mt_brands` (
  `brand_id` INT(14) NOT NULL AUTO_INCREMENT,
  `brand_name` VARCHAR(255) NOT NULL DEFAULT '',
  `brand_photo` VARCHAR(255) NOT NULL,
  PRIMARY KEY (`brand_id`));
